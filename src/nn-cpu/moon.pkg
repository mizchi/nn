import {
  "mizchi/numbt" @numbt,
  "mizchi/nn/nn" @nn,
}

import {
  "mizchi/nn" @wgpu,
} for "test"

options(
  link: { "native": { "cc-link-flags": "-framework Accelerate" } },
  "supported-targets": [ "native" ],
)
