import {
  "mizchi/numbt" as @numbt,
  "mizchi/wgpu" as @wgpu,
  "mizchi/wgpu/nn" as @nn,
}

options(
  link: { "native": { "cc-link-flags": "-framework Accelerate" } },
  "supported-targets": [ "native" ],
)
