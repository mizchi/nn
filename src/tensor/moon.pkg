import {
  "moonbitlang/core/math" as @math,
}

options(
  link: { "native": { "cc-link-flags": "-framework Accelerate" } },
  "native-stub": [ "blas_stub.c" ],
  targets: { "blas.mbt": [ "native" ], "vit_profile.mbt": [ "native" ] },
)
