import {
  "mizchi/nn/tensor" as @tensor,
  "moonbitlang/async",
  "moonbitlang/async/fs" as @afs,
  "moonbitlang/core/env" as @env,
  "moonbitlang/core/json" as @json,
  "moonbitlang/core/strconv" as @strconv,
}

options(
  "is-main": true,
  link: { "native": { "cc-link-flags": "-framework Accelerate" } },
  targets: { "main.mbt": [ "not", "js" ] },
)